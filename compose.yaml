services:
  mcp-workbench:
    build: .
    ports:
      - "127.0.0.1:5070:8080"
    environment:
      Security__ApiKey: "${MCP_WORKBENCH_API_KEY:-}"
    volumes:
      - mcp-workbench-data:/app/data
    restart: unless-stopped

volumes:
  mcp-workbench-data:
